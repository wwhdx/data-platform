# 开发模式 Dockerfile（挂载源码 + 热重载）
FROM node:20-alpine

RUN corepack enable && corepack prepare pnpm@10.31.0 --activate

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY . .

EXPOSE 3400

CMD ["pnpm", "dev"]
