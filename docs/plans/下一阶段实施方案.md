# 下一阶段实施方案

> 2026-05-19 · 配置 v1.1（B9–B11）+ PubMed（A6）已落地；下文区分「已完成」与「待做」  
> **代码/文档对照真源**：[实施进度总览.md](./实施进度总览.md) §2 代码真源 + **§4 任务计划**  
> **任务计划定稿**：2026-05-19（C2→C3 优先；A5/B13/A4 第二周）

---

## 1. 进度盘点

### 1.1 已实现

| 模块 | 组件 | 状态 |
|------|------|------|
| Connector | OpenAlexConnector | ✅ |
| Connector | CrossRefConnector | ✅ |
| Connector | WorldBankConnector | ✅（`src/index.ts` 已注册；DB 默认 `disabled`） |
| Connector | PubMedConnector | ✅ 2026-05-19（`ncbi_eutils` + `options.entrez_db`） |
| Connector | BaseConnector + `paginateOffset()` | ✅ |
| Config | interface_profile v1.1 + expand + CLI | ✅ B9–B11 |
| Config | `resolveRuntimeConfig` + `factory.ts` | ✅ B11（含原 B6） |
| Connector | RateLimiter + ExponentialBackoff | ✅ |
| Config | `config/sources.yml` + loader + syncToDb | ✅ |
| Config | `PUT /api/admin/sources/:id` + `config_audit_log` | ✅（`005_config.sql`） |
| RAG | 多后端 Embedding (ollama/openai/voyage) | ✅ |
| RAG | pgvector + tsvector + RRF (`hybridSearch`) | ✅ |
| Processor | dedup + 批量 200 条（Scheduler 缓冲） | ✅ |
| Storage | migrations `001`–`006` | ✅ |
| API | `GET /health`、`/api/search`、`/api/admin/*`（含 `/schedules`） | ✅ |
| CLI | 9 顶层 + `config` 子命令；`schedules --live` | ✅ |
| Scheduler | B13 YAML cron + B14 可观测 | ✅ |
| Test | **98** tests；smoke inject（`src/__tests__/smoke/`） | ✅ |
| Test live | `pnpm smoke:live`（需 DB :5433） | ✅ 脚本 |
| E2E | Phase 1 四步 curl（见 §1.3） | ✅ 2026-05-19 |

### 1.2 已设计、未实现

**来自《数据源接入与RAG构建方案》：**

| ID | 条目 | 设计优先级 | 工作量 |
|----|------|----------|--------|
| ~~A1~~ | ~~Scheduler 批量消费~~ | — | **已完成** |
| ~~A2~~ | ~~`paginateOffset()`~~ | — | **已完成** |
| ~~A3~~ | ~~World Bank Connector~~ | — | **已完成** |
| A4 | Semantic Scholar Connector | P1 | ~100 行 |
| ~~A5~~ | ~~增量采集 `last_collected_at` 持久化~~ | — | **✅ 2026-05-19** `007_incremental_schedule.sql` |
| ~~A6~~ | ~~PubMed Connector~~ | — | **✅ 2026-05-19** |
| A7 | arXiv OAI-PMH Connector | P2 | ~150 行；`id: arxiv_oai` |
| A8 | Stage 3 分块策略 | P2 | ~80 行 |
| A9 | Embedding 队列化 + 重试 | P3 | 需新表 |

**来自《外部数据源配置热更新方案》：**

| ID | 条目 | 设计优先级 | 工作量 |
|----|------|----------|--------|
| ~~B1–B5~~ | ~~YAML + sync + PUT + audit~~ | — | **已完成** |
| ~~B6~~ | ~~Connector 读 DB 配置~~ | — | **✅ 并入 B11** |
| ~~B7~~ | ~~CLI diff/sync/export~~ | — | **✅ 并入 B10** |
| B8 | 健康检查外部 API 探活 | P2 | ~40 行 |
| ~~B9~~ | ~~interface_profile + expand~~ | — | **✅ 2026-05-19** |
| ~~B10~~ | ~~CLI validate / by-profile / sync~~ | — | **✅ 2026-05-19** |
| ~~B11~~ | ~~resolveRuntimeConfig~~ | — | **✅ 2026-05-19** |
| ~~B12~~ | ~~interface_profile 文档~~ | — | **✅ 2026-05-19** |
| **B13** | **YAML 驱动 cron（`enabled` + `schedule`）** | ✅ | 路线 A |
| **B14** | **调度可观测 CLI + live API** | ✅ | `pnpm cli schedules` |

**接父仓前门禁**：`pnpm test:run`（L0）→ **`pnpm test:integration`（L2-fast I 轨，推荐）** → 可选 `pnpm smoke:live`。

**来自《集成测试最小闭环方案》（I 轨，不依赖父仓）：**

| ID | 条目 | 设计优先级 | 工作量 |
|----|------|----------|--------|
| I1 | `EMBED_BACKEND=mock` | P0 | ~2h |
| I2 | `FixtureConnector` | P0 | ~1h |
| I3 | integration harness | P0 | ~1h |
| I4 | `pipeline-closed-loop.test.ts` | P0 | ~1h |
| I5 | `scripts/e2e-loop.sh` | P1 | ~0.5h |

详案：[集成测试最小闭环方案.md](./集成测试最小闭环方案.md)

**来自《平台接入设计框架》：**

| ID | 条目 | 设计优先级 | 工作量 |
|----|------|----------|--------|
| ~~C1~~ | ~~Docker compose~~ | — | **已完成** |
| C2 | 主平台 `DataPlatformClient` | Phase 0 | ~50 行 |
| C3 | engine-core `searchProvider` 注入 data-platform | Phase 1 | ~20 行 |
| C4 | 管理后台「数据源」页面 | Phase 2 | ~120 行 |

### 1.3 E2E 验收（2026-05-19）

与 `docs/phase1-plan.md` §六 一致；本地 `localhost:5433` + `pnpm dev`。

| 步骤 | 结果 |
|------|------|
| `POST /api/search`（采集前） | ✅ |
| `POST /api/admin/collect` openalex | ✅ 8 条新文档 |
| `GET /api/admin/jobs` | ✅ |
| `POST /api/search`（采集后） | ✅ |
| `GET /health` | ✅ `ok:true` |

---

## 2. 依赖关系分析

```
A2 (paginateOffset)
  ├── A3 (World Bank Connector)  ← 依赖 A2
  └── A4 (Semantic Scholar)      ← 不依赖 A2，有自己的分页

A1 (批量消费) — 无依赖，独立改动

B1-B5 (配置热更新 P0) — 无依赖，独立模块

B6 (Connector 读数据库) ← 依赖 B1+B3（配置文件+表结构就位）

B9 (interface_profile + expand) — 无依赖；**应在 B7/B10 的 sync/diff 之前完成**

B10 (CLI validate / by-profile) ← 依赖 B9

B11 ✅ 已完成

B13 (YAML 驱动 cron) ← 依赖 B9–B11（展开配置）；与 A5 可并行

C2 (DataPlatformClient) — 父仓，无依赖

C3 (engine-core 注入) ← 依赖 C2 + data-platform 有检索数据（openalex/crossref 等）

I1–I4 (集成测试最小闭环) — 无父仓依赖；可与 C2 并行；验证 SearchProvider HTTP 契约
```

**关键路径**：接父仓前可先 **I1→I4** 验证子包闭环，再 **C2→C3**。

**独立可并行**：A1（批量消费）和 B1-B5（配置热更新）和 C1-C2（Docker 部署）三者互不依赖。

---

## 3. 推荐实施方案

### ~~已完成里程碑~~（截至 2026-05-19）

| 批次 | 任务 |
|------|------|
| Phase 1 | B1–B5、A1–A3、E2E、Docker、RAG MVP |
| 配置 v1.1 | B9–B12、B6/B7（合入 B10/B11） |
| Connector | A6 PubMed；运行时 4 源（openalex / crossref / worldbank / pubmed） |

### 当前优先：平台闭环（第 1 周前半）

| 步骤 | 任务 | 文件（父仓 / engine-core） | 验收 |
|------|------|---------------------------|------|
| 1 | **C2** `DataPlatformClient` | `src/lib/dataPlatform.ts`（规划路径，见 [平台接入设计框架](./平台接入设计框架.md) §4） | `DATA_PLATFORM_URL` + `search()` |
| 2 | **C3** `searchProvider` 工厂 | `engine-core` services / adapter | 工作流命中 `POST /api/search` |

**交付**：望野内容生成链路可选用 data-platform 知识库，与 Serper 并存。

### 第 1 周后半：采集与调度（data-platform 子包，可与 C2/C3 并行）

| 步骤 | 任务 | 要点 |
|------|------|------|
| 3 | **A5** 增量采集 | `collection_schedules.last_collected_at`；`runCollection` 传 `since` |
| 4 | ~~**B13**~~ YAML 驱动 cron | ✅ `registerSchedulesFromConfig`；仅 `enabled` + 已注册 Connector + 合法 `schedule` |

**B13 设计要点**（避免误调度）：

- 不以「仅 `registerConnector`」作为 cron 条件。
- `sources.yml` 中 `enabled: false` 的源（如 pubmed）**不得**自动 `schedule()`，除非运维显式启用并 sync。
- `schedule` 字段来自展开后配置或 `collection_schedules` 表，与 [外部数据源配置热更新方案](./外部数据源配置热更新方案.md) 一致。

### 第 2 周：扩源 + 质量

| 步骤 | 任务 | 要点 |
|------|------|------|
| 5 | **A4** Semantic Scholar | `profile: rest_header_custom`；`SEMANTIC_SCHOLAR_API_KEY`；默认 disabled |
| 6 | **B8**（可选） | 各源轻量 HTTP 探活写入 `/health` |
| 7 | **A8** 或 **A7** | **A8**：`processors/chunk.ts` 论文分段；**A7**：`arxiv_oai` + `oai_pmh` profile |

### 第三周起：平台 UI 与长尾源

| 任务 | 说明 |
|------|------|
| **C4** | 望野 admin 数据源页；依赖 C2 |
| 其余 Connector | patentsview / sec_edgar / fred 等按业务选 1–2 个，非 12 源齐做 |
| **A9** | **暂缓**（见 §5） |

### 依赖总览

```
C2 → C3

B9–B11（已完成）→ B13
A5（独立，建议与 B13 同周）

A4（独立，第 2 周）
A8 / A7（有数据后，第 2 周二选一）
C4 ← C2
```

> 任务优先级表与决策矩阵：[实施进度总览 §4](./实施进度总览.md#4-下一阶段任务计划2026-05-19-定稿)

---

## 4. 第一周详细任务拆解

### 任务 1：配置热更新 P0

| 步骤 | 文件 | 内容 |
|------|------|------|
| 1.1 | `config/sources.yml`（新增） | 12 个数据源的完整 YAML 配置（含 hackernews；直接从方案文档复制） |
| 1.2 | `src/config/loader.ts`（新增） | `loadConfig(path)` → 解析 YAML → 校验 → 返回 `DataPlatformConfig` |
| 1.3 | `src/config/sync.ts`（新增） | `syncToDb(config)` → 遍历 sources → `INSERT ON CONFLICT UPDATE` |
| 1.4 | `src/storage/migrations/005_config.sql`（新增） | `config_audit_log` 表 + `data_sources.updated_at` 字段 |
| 1.5 | `src/api/routes/admin.ts` | 新增 `PUT /sources/:id` + 审计日志写入 |
| 1.6 | `src/index.ts` | 启动时调用 `syncToDb(loadConfig("config/sources.yml"))` |
| 1.7 | `src/cli/index.ts` | 新增 `config list` 命令（读取 DB 输出表格） |

**依赖**：需要 `js-yaml` 库（`pnpm add js-yaml && pnpm add -D @types/js-yaml`）

### 任务 2：paginateOffset + World Bank Connector

| 步骤 | 文件 | 内容 |
|------|------|------|
| 2.1 | `src/connectors/base.ts` | 新增 `paginateOffset<T>(fetchPage, opts)` 方法 |
| 2.2 | `src/connectors/worldbank.ts`（新增） | WorldBankConnector 完整实现 |
| 2.3 | `src/connectors/index.ts` | +1 export |
| 2.4 | `src/index.ts` | 注册 + cron（周日凌晨） |
| 2.5 | `src/storage/migrations/006_worldbank.sql`（新增） | data_sources INSERT |
| 2.6 | `src/__tests__/unit/worldbank.test.ts`（新增） | ~15 tests |

### 任务 3：Scheduler 批量消费

| 步骤 | 文件 | 内容 |
|------|------|------|
| 3.1 | `src/scheduler/index.ts` | `runCollection()` 增加 `buffer[]`，累积 200 条再调用 `dedup(buffer)` |

---

## 5. 不做的事（明确排除）

| 排除项 | 理由 |
|--------|------|
| Embedding 队列化 | 日均采集量 < 1000 条时不需要，当前 fire-and-forget + 日志够用 |
| Stage 3 段落级分块 | 需要先有长文本数据（当前仅 title+abstract），分块策略收益有限 |
| Stage 2 LLM 富化 | 属于 engine-core 侧能力，data-platform 只做规则/NER 轻量抽取 |
| `/api/context` 端点 | 已明确移除，由 engine-core summarizeContext 替代 |
| 知识图谱 (Neo4j) | Phase 3，需要 Connector 覆盖先到位 |

---

## 6. 风险与缓解

| 风险 | 缓解 |
|------|------|
| World Bank API 无限制 → 可能被限流 | `paginateOffset` 中每页之间加 `sleepMinInterval` |
| Semantic Scholar 商业授权未完成 | 默认 `enabled: false`，仅开放给非商业使用 |
| YAML 解析库新增外部依赖 | `js-yaml` 是 Node.js 生态事实标准，零原生依赖 |
| 配置文件与数据库不同步 | `config diff` 命令 + 启动时 `config validate` 检查 |

---

## §变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-05-15 | 初稿与两周排期 |
| v0.2 | 2026-05-19 | 第一周 A1–A3/B1–B5 标完成；§1.3 E2E；优先项改为 C2–C3；测试数 57 |
| v0.3 | 2026-05-19 | 增 B9–B12（interface_profile 配置分层）；Day 9–10 排期调整；链至实施方案 v1.0 |
| v0.3.1 | 2026-05-19 | 项目设计文档迁至 `docs/plans/`；`docs/knowledge/` 仅保留共识知识 |
| v0.4 | 2026-05-19 | 新增 [实施进度总览](./实施进度总览.md)；§1.1 补 CLI health、文档目录；与代码 HEAD 对齐 |
| v0.4.1 | 2026-05-19 | 12 源计数；B11 吞并 B6；Day 9–10 排期与 interface-profile v1.0.3 对齐 |
| v0.5 | 2026-05-19 | §3 重写：B9–B11/A6 已完成；新增 B13；两周排期 C2→C3 / A5 / A4；链至实施进度总览 §4 |
| v0.6 | 2026-05-19 | B13/B14 完成；smoke inject + smoke:live；§1.1 测试 98；接父仓前门禁 |
| v0.7 | 2026-05-19 | 新增 I 轨 [集成测试最小闭环方案](./集成测试最小闭环方案.md)；§2 依赖补 I1–I4；门禁 L2-fast |
